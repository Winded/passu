#!/usr/bin/env node

const passu = require('../src/index');

let password = passu.hashPassword('myPassword');

let db = passu.createNewDatabase(password);
let bytes = passu.saveDatabase(db);

console.log(bytes);